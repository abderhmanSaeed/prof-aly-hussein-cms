{
  "DetailedErrors": false,
  "AdminAccount": {
    "Email": "admin@aly-hussein.local",
    "//Password": "Default Super Admin password used ONLY to seed the account on the FIRST startup of an empty database. User Secrets are NOT loaded in Production, so the password must live here or in the AdminAccount__Password environment variable (which overrides this value). It is never re-applied to an existing account. ROTATE it from Admin > System after the first login.",
    "Password": "Admin#2026Dev"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "ProfAly": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    },
    "File": {
      "Directory": "Logs",
      "MinLevel": "Information",
      "DailyRolling": false
    }
  },
  "Backup": {
    "Enabled": true,
    "KeepLast": 0
  },
  "RateLimiting": {
    "GlobalPermitLimit": 240,
    "LoginPermitLimit": 10
  }
}
